<form method="post" action="{{ path('repair_delete', {'id': repair.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repair.id) }}">
    <button class="btn">Delete</button>
</form>
