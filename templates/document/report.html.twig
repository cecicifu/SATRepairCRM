<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>SATRepairCRM</title>
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0">

    {% block stylesheets %}
        {% for stylesheet in sonata_config.getOption('stylesheets', []) %}
            <link rel="stylesheet" href="{{ absolute_url(asset(stylesheet)) }}">
        {% endfor %}
    {% endblock %}
</head>
<body>
    <main>
        <div class="row">
            <div class="col-md-12">
                <img src="{{ absolute_url(asset('build/images/logo.png')) }}" alt="SATRepairCRM Logo" style="margin-left: 100px">
            </div>
        </div>
        <div>
            <div class="row">
                <div class="col-xs-4">
                    <b>Code:</b>
                    <span>{{ repair.code }}</span>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-4">
                    <b>Date:</b>
                    <span>{{ repair.created|date("m/d/Y") }}</span>
                </div>
            </div>
        </div>
        <div style="margin-top: 10px;">
            <div class="row">
                <div class="col-xs-7">
                    <b>Customer:</b>
                    <span>{{ repair.customer }}</span>
                </div>
                <div class="col-xs-5">
                    <b>Phone:</b>
                    <span>{{ repair.customer.phone }}</span>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <b>Category:</b>
                    <span>{{ repair.category }}</span>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-7">
                    <b>Colour:</b>
                    <span>{{ repair.colour }}</span>
                </div>
                <div class="col-xs-5">
                    <b>IMEI:</b>
                    <span>{{ repair.imei }}</span>
                </div>
            </div>
        </div>
        <div style="margin-top: 10px;padding-bottom: 100px;border-bottom: 1px solid #AAAAAA;">
            <div class="row">
                <div class="col-xs-12">
                    <b>Fault:</b>
                    <span>{{ repair.fault }}</span>
                </div>
            </div>
            <div class="row">
                <div class="col-xs-12">
                    <b>Pattern:</b>
                    <span>{{ repair.pattern }}</span>
                </div>
            </div>
        </div>
        <div style="margin-bottom: 10px;border-bottom: 1px solid #AAAAAA;">
            <div class="row">
                <div class="col-xs-12">
                    <small>
                        Attended by:
                    </small>
                </div>
            </div>
            <div class="row" style="padding-top: 80px;margin-bottom: 10px;">
                <div class="col-xs-12 text-right">
                    Customer sign:_______________
                </div>
            </div>
        </div>
        <div>
            <div class="row">
                <div class="col-xs-12">
                    <small><p>The warranty period for repairs will be <b>6 months</b>.</p></small>
                </div>
                <div class="col-xs-12" style="margin-top: 20px;">
                    <small><p>Failures caused by malware, viruses or other malicious software are exempt from warranty.</p></small>
                </div>
            </div>
        </div>
    </main>
</body>
</html>